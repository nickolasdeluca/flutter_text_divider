/// A Flutter package providing a [TextDivider] widget — a divider with a
/// text label centred between two lines.
///
/// Supports both [Axis.horizontal] and [Axis.vertical] orientations, and
/// integrates with Flutter's [ThemeData] and [DividerThemeData] by default.
library;

export 'src/flutter_text_divider.dart';
